language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t omarzaam/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true omarzaam/docker-react npm run test